version: '3'

services:
  go-wecomchan:
    # 使用预构建镜像（推荐）
    image: gowfqk/go-wecomchan:latest

    # 如果需要本地构建，注释上面的image，取消下面的build注释
    # build: .

    environment:
      - SENDKEY=204800
      - WECOM_CID=ww0a5b8d4134eff2f9
      - WECOM_SECRET=3AoCldfNpZfRLDCxV-m6QLZfQWohx0PA_ei6egSKCx0
      - WECOM_AID=1000003
      - WECOM_TOUID=@all
      # 缓存类型: none(不缓存), memory(内存缓存), redis(Redis缓存)
      - CACHE_TYPE=memory
    ports:
      - 3000:8080
    restart: unless-stopped
